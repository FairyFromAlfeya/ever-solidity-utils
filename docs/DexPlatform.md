### DexPlatform

```mermaid
classDiagram   
    class DexPlatform {
        +address root
        +uint8 type_id
        +TvmCell params
        +constructor(TvmCell, uint32, address, address)
    }
```
