pragma ever-solidity >= 0.61.2;

/// @author Alexander Hramcov
/// @title DEX Platform Types
/// @notice Different contract types of DEX for deploying via DexPlatform
library DexPlatformTypes {
    uint8 constant ACCOUNT = 1;
    uint8 constant POOL = 2;
}
